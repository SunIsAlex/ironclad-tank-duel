# GLM-5.2 完整游戏开发提示词

你是一名资深 HTML5 游戏工程师、TypeScript 架构师、移动端 Web 性能优化专家和游戏 UI/UX 设计师。

请直接为我开发一个**可完整运行的、原创美术风格的、离线本地双人回合制 2D 坦克炮战游戏**。游戏核心体验参考经典横版坦克弹道对战游戏，例如：

* 坦克位于可破坏地形上
* 玩家调整炮管角度和发射力度
* 炮弹受重力和风力影响
* 炮弹命中后造成范围伤害并炸出弹坑
* 两名玩家轮流行动
* 击败对方坦克后获胜

游戏仅参考这一玩法类型，不得复制任何现有游戏的源代码、地图、美术、角色造型、武器名称、音效、商标、界面布局或其他受版权保护的内容。不要使用“ShellShock Live”作为正式游戏名称。请创建原创名称、原创视觉风格和原创武器命名。

---

# 一、项目目标

开发一个无需服务器、无需联网、无需登录的本地双人游戏。

两名真人玩家使用**同一台电脑或同一台安卓设备**轮流操作，属于本地 Hot-seat 模式。

必须满足：

1. 支持桌面端 Chrome、Edge 等现代浏览器。
2. 支持新版 Android Chrome。
3. 支持鼠标、键盘和触摸屏。
4. 两名玩家使用完全相同的操作方式。
5. 不得给玩家 1 和玩家 2 设置两套不同按键。
6. 当前轮到哪名玩家，公共控制器就控制哪名玩家。
7. 无后端、无联网对战、无 WebSocket、无账号系统。
8. 支持安装为 PWA，并在首次加载后离线运行。
9. 项目必须是真正可以启动、构建和游玩的完整项目，不能只提供演示片段或伪代码。

---

# 二、指定技术栈

请使用以下技术栈，不要擅自更换：

* Vite
* TypeScript
* HTML5 Canvas 2D
* CSS3
* Web Audio API
* Pointer Events
* localStorage
* PWA Service Worker

尽量不使用大型第三方游戏引擎，以降低安卓浏览器运行负担。

允许使用轻量级 npm 工具，但核心玩法、弹道、碰撞、地形破坏、回合管理和输入系统必须自行实现。

项目应通过以下命令运行：

```bash
npm install
npm run dev
```

并可通过以下命令生产构建：

```bash
npm run build
npm run preview
```

---

# 三、游戏模式

游戏只需要实现：

## 本地双人对战

* 玩家 1 和玩家 2 坐在同一设备前轮流操作。
* 每一回合只有当前玩家的坦克能够移动、瞄准和开火。
* 炮弹发射后，所有操作按钮暂时锁定。
* 必须等待炮弹爆炸、伤害结算、地形稳定和坦克下落结束后，才切换到下一名玩家。
* 游戏不需要电脑 AI。
* 游戏不需要网络模式。
* 游戏不需要匹配系统。
* 游戏不需要服务器。

---

# 四、完整回合流程

请使用明确的状态机管理回合，至少包含以下阶段：

```text
GAME_START
TURN_START
PLAYER_CONTROL
PROJECTILE_FLYING
EXPLOSION
DAMAGE_RESOLUTION
TERRAIN_SETTLING
TURN_END
GAME_OVER
PAUSED
```

标准回合流程：

1. 显示当前玩家提示。
2. 重置当前玩家本回合的可移动距离。
3. 当前玩家可以移动坦克。
4. 当前玩家可以调整炮管角度。
5. 当前玩家可以调整发射力度。
6. 当前玩家选择武器。
7. 当前玩家按下发射按钮。
8. 锁定所有操作。
9. 炮弹按照弹道飞行。
10. 炮弹与地形、坦克或边界发生碰撞。
11. 播放爆炸效果。
12. 修改地形，生成弹坑。
13. 计算伤害和击退效果。
14. 检查坦克下方是否失去支撑。
15. 让悬空坦克自然下落到新的地形表面。
16. 检查是否有玩家生命值归零。
17. 未分出胜负时切换玩家。
18. 开始下一回合。

任何时候都不能出现两名玩家同时操作的情况。

---

# 五、玩家操作要求

## 关键要求

两名玩家必须使用**完全相同的操作逻辑、按键和触控界面**。

玩家切换后：

* 控制按钮位置不变。
* 键盘按键不变。
* 鼠标操作不变。
* 触控操作不变。
* 只有当前玩家的坦克颜色、名字和回合提示发生变化。
* 不得设置“玩家 1 使用 WASD，玩家 2 使用方向键”这种不同操作方式。

## 电脑端键盘操作

默认操作：

```text
A / 左方向键：向左移动
D / 右方向键：向右移动
W / 上方向键：增大炮管角度
S / 下方向键：减小炮管角度
鼠标滚轮向上：提高发射力度
鼠标滚轮向下：降低发射力度
空格键：发射
Tab：切换武器
Esc：暂停或继续
R：游戏结束后重新开始
```

需要同时支持：

* 长按按键连续调整。
* 点击按键进行小幅调整。
* 阻止空格键和方向键导致网页滚动。
* 输入框或菜单获得焦点时，不误触发游戏操作。

## 鼠标操作

支持鼠标点击屏幕控制按钮。

可以增加以下瞄准方式：

* 鼠标拖动瞄准指示线。
* 拖动炮管附近区域调整角度。
* 拖动力度条调整力度。
* 点击发射按钮开火。

鼠标瞄准不能替代键盘操作，两种方式必须同时可用。

## 安卓触摸操作

使用 Pointer Events 统一处理鼠标和触摸输入。

屏幕底部显示固定触控控制面板：

左侧控制区：

* 向左移动
* 向右移动

中间控制区：

* 角度减小
* 角度增大
* 力度滑块或力度减小、增大按钮

右侧控制区：

* 武器切换
* 发射按钮

移动端要求：

* 每个主要触控按钮点击区域不得小于 48×48 CSS 像素。
* 发射按钮应更大、更醒目。
* 支持按钮长按。
* 阻止游戏区域中的页面滚动、文本选择、双击缩放和长按菜单。
* 正确处理 `pointerdown`、`pointermove`、`pointerup` 和 `pointercancel`。
* 玩家手指移出按钮后必须停止连续操作。
* 页面切换到后台时，必须清除所有持续输入状态。
* 不依赖 hover。
* 不使用只能由鼠标右键触发的功能。
* 适配 Android Chrome 的浏览器工具栏高度变化。
* 适配刘海屏和底部手势条，使用 `env(safe-area-inset-*)`。
* 音频必须在用户首次点击后解锁。

---

# 六、屏幕方向与响应式布局

游戏优先使用横屏布局。

必须支持以下情况：

## 横屏

横屏是主要游戏模式：

* Canvas 尽可能占满可用区域。
* 顶部显示玩家信息、风力和回合信息。
* 底部显示控制器。
* 游戏区域不能被控制面板遮挡。

## 竖屏

不能直接崩溃或出现严重错位。

竖屏时可以：

* 显示“建议旋转设备获得更好体验”的半透明提示。
* 同时保持游戏仍然可以操作。
* 自动缩放 HUD 和控制按钮。
* 保证按钮不会超出屏幕。

## 最低适配尺寸

至少适配：

```text
360 × 640
390 × 844
412 × 915
640 × 360
844 × 390
1280 × 720
1366 × 768
1920 × 1080
```

正确处理：

* `window.resize`
* `orientationchange`
* `devicePixelRatio`
* 浏览器地址栏收起或展开
* 全屏模式
* Canvas 高分辨率渲染

Canvas 的内部像素尺寸应结合 `devicePixelRatio` 设置，但必须限制最大像素比，避免高分辨率安卓设备性能过低。

建议最大渲染像素比设为 2。

---

# 七、游戏世界

## 地图

每局自动生成一张原创随机地图。

地图要求：

* 横向卷轴式 2D 地形。
* 使用平滑的随机高度曲线。
* 地形不能出现大量过于尖锐的垂直断层。
* 两辆坦克生成点必须存在足够宽的落脚区域。
* 两辆坦克之间保持合理距离。
* 坦克不能出生在地图外或彼此重叠。
* 随机地图必须可以通过种子复现。
* 在结算页面显示地图种子。
* 支持输入种子重新生成相同地图。

可使用：

* 多层正弦波
* 插值噪声
* 平滑随机采样
* 一维分形噪声

不要依赖外部地图图片。

## 地形视觉

使用原创程序化绘制：

* 天空渐变
* 远景山脉
* 云层或星空
* 地表草层
* 土壤层
* 地形边缘高光
* 简单粒子效果

不得使用原游戏截图或素材。

---

# 八、可破坏地形

这是核心功能，必须认真实现。

建议方案：

1. 使用独立的离屏 Canvas 保存地形。
2. 使用 alpha 通道或像素掩码表示实体地形。
3. 爆炸时通过圆形或不规则形状从地形中擦除区域。
4. 使用 `globalCompositeOperation = "destination-out"` 制造弹坑。
5. 同步更新碰撞检测使用的地形掩码。
6. 炮弹碰撞检测不能只依赖视觉 Canvas。
7. 坦克落地检测必须根据最新地形轮廓计算。

必须支持：

* 圆形弹坑
* 多重爆炸弹坑
* 连续爆炸
* 坦克下方地形被炸空
* 坦克沿斜坡缓慢滑落或垂直落下
* 坦克不会永久悬浮
* 坦克不会掉进地形内部
* 坦克不会因为弹坑边缘产生无限抖动

为提升性能：

* 不要每帧读取整张 Canvas 的全部像素。
* 可以维护低分辨率碰撞掩码。
* 只更新爆炸影响的局部矩形区域。
* 使用缓存的地表高度数据。
* 在地形改变后，仅重新计算受影响的横坐标范围。

---

# 九、坦克系统

每名玩家拥有一辆坦克。

坦克属性：

```ts
interface Tank {
  id: string;
  playerIndex: number;
  name: string;
  x: number;
  y: number;
  velocityX: number;
  velocityY: number;
  bodyAngle: number;
  turretAngle: number;
  power: number;
  health: number;
  maxHealth: number;
  movementFuel: number;
  isGrounded: boolean;
  selectedWeaponId: string;
}
```

坦克要求：

* 根据所在位置的地形坡度自动调整车身角度。
* 炮管角度与车身角度正确叠加。
* 坦克移动时不能穿过陡峭地形。
* 每回合拥有有限移动燃料。
* 移动燃料耗尽后仍可瞄准和发射。
* 发射后不能继续移动。
* 坦克下落时可受到坠落伤害，但伤害应适中。
* 坦克生命值初始为 100。
* 坦克受到爆炸伤害时可产生轻微击退。
* 坦克颜色明显区分，但整体造型保持原创。
* 坦克死亡时播放非写实、非血腥的爆炸动画。

建议玩家颜色：

* 玩家 1：蓝色或青色
* 玩家 2：橙色或红色

颜色之外还要使用 P1、P2 标签，照顾色觉差异用户。

---

# 十、弹道与物理

炮弹使用固定时间步长或稳定的 delta time 更新。

基本弹道参数：

```text
水平速度 = cos(发射角度) × 发射力度
垂直速度 = -sin(发射角度) × 发射力度
每帧增加重力影响
每帧增加风力产生的水平加速度
```

实现要求：

* 弹道在不同帧率下基本一致。
* 不使用完全依赖帧数的移动方式。
* 炮弹速度较快时使用分段检测或连续碰撞检测，避免穿透薄地形。
* 炮弹可与地形碰撞。
* 炮弹可与两辆坦克碰撞。
* 炮弹飞出地图较远后自动销毁并结束结算。
* 炮弹飞行时间必须设置合理上限。
* 不得因为炮弹未销毁导致回合永久卡死。
* 风力在每回合开始时发生变化。
* 风力既可能向左，也可能向右。
* HUD 显示风向和强度。
* 可以显示一小段预测轨迹，但预测轨迹不能完全延伸到最终落点。
* 预测轨迹应使用半透明圆点。
* 切换武器后，预测轨迹需要根据武器属性更新。

---

# 十一、原创武器系统

至少实现以下 8 种原创武器，名称可以进一步美化，但不得直接复制现有游戏武器名称。

## 1. 基础炮弹

* 单发炮弹
* 中等伤害
* 中等爆炸范围
* 无限弹药或大量弹药

## 2. 三连散射弹

* 同时发射三枚炮弹
* 三枚炮弹角度略有不同
* 单枚伤害低于基础炮弹

## 3. 空中分裂弹

* 飞行一段时间后自动分裂
* 分裂为多枚小型弹体
* 小型弹体分别受重力影响

## 4. 弹跳炮弹

* 首次接触地形时以衰减速度反弹
* 达到最大反弹次数或速度过低后爆炸

## 5. 延迟爆破弹

* 接触地面后短暂停留
* 显示明显倒计时
* 倒计时结束后爆炸

## 6. 钻地弹

* 接触地形后继续向内部移动一小段距离
* 随后在地下爆炸
* 制造较深弹坑

## 7. 重型冲击弹

* 爆炸范围较大
* 伤害较高
* 弹药数量较少
* 弹体重量更大，飞行弧线更低

## 8. 微型集束弹

* 主弹在空中爆开
* 向下释放若干小型子弹
* 子弹数量需限制，避免移动端性能过低

武器数据采用配置驱动：

```ts
interface WeaponDefinition {
  id: string;
  displayName: string;
  description: string;
  ammo: number;
  projectileCount: number;
  projectileSpeedMultiplier: number;
  gravityMultiplier: number;
  windMultiplier: number;
  explosionRadius: number;
  maxDamage: number;
  terrainDamageMultiplier: number;
  fuseTime?: number;
  bounceCount?: number;
  splitTime?: number;
}
```

不要把所有武器逻辑写在一个巨大的 `if/else` 中。

使用：

* 武器定义表
* 弹体行为接口
* 独立行为模块
* 通用爆炸系统

---

# 十二、伤害系统

爆炸伤害根据坦克距离爆炸中心的距离衰减。

建议公式：

```text
distanceRatio = distance / explosionRadius
damage = maxDamage × (1 - distanceRatio)
```

要求：

* 距离超过爆炸半径时不造成伤害。
* 伤害不能为负数。
* 显示浮动伤害数字。
* 直接命中可以获得少量伤害奖励。
* 可以伤害自己。
* 同一次爆炸不能对同一辆坦克重复结算。
* 多枚炮弹可以分别造成伤害。
* 伤害结算结束后再判断游戏胜负。
* 两名玩家同时被击败时显示平局。

---

# 十三、移动系统

每回合给予当前玩家有限移动燃料。

要求：

* 左右移动都会消耗燃料。
* 移动速度适中。
* 陡坡上移动速度降低。
* 坡度超过限制时不能继续攀爬。
* 坦克不能移动到世界边界外。
* 坦克不能穿过另一辆坦克。
* 移动结束后坦克自动贴合地形。
* 移动燃料使用进度条显示。
* 回合切换时为下一名玩家重置燃料。

移动不是游戏的主要攻击方式，因此移动距离不应过大。

---

# 十四、摄像机

实现平滑摄像机系统。

摄像机行为：

* 当前玩家操作时，镜头关注当前坦克。
* 炮弹发射后，镜头跟随炮弹。
* 多枚炮弹存在时，优先跟随主弹或所有弹体的包围中心。
* 爆炸发生时镜头短暂聚焦爆炸位置。
* 结算完成后平滑移动到下一名玩家。
* 镜头不能显示地图外过多空白区域。
* 支持轻微屏幕震动。
* 屏幕震动强度可在设置中关闭或降低。
* 移动端避免强烈或长时间震动效果。

地图宽于屏幕时允许摄像机横向移动。

不要依赖网页本身的横向滚动。

---

# 十五、HUD 和界面

## 顶部 HUD

显示：

* 玩家 1 名称和生命值
* 玩家 2 名称和生命值
* 当前玩家
* 回合数
* 剩余操作时间
* 风向
* 风力强度
* 当前武器
* 当前武器剩余数量
* 当前角度
* 当前力度

## 回合提示

每回合开始时，在屏幕中央显示短暂提示：

```text
玩家 1 的回合
```

或：

```text
玩家 2 的回合
```

提示显示约 1 秒后淡出，不能长时间遮挡画面。

## 主菜单

包含：

* 开始本地双人游戏
* 玩家名称设置
* 地图种子设置
* 游戏规则设置
* 音效设置
* 操作说明
* 关于游戏

## 游戏设置

至少包括：

* 每回合时间：无限、30 秒、45 秒、60 秒
* 初始生命值
* 风力强度
* 移动燃料
* 屏幕震动
* 音乐音量
* 音效音量
* 是否显示预测轨迹
* 地图种子

设置存储到 localStorage。

## 暂停菜单

包含：

* 继续游戏
* 重新开始本局
* 返回主菜单
* 音量设置
* 操作说明

## 游戏结束页面

显示：

* 获胜玩家
* 平局情况
* 总回合数
* 两名玩家造成的总伤害
* 命中次数
* 直接命中次数
* 地图种子
* 再来一局
* 返回主菜单

---

# 十六、视觉效果

全部使用原创的程序化图形，不依赖受版权保护的素材。

至少实现：

* 炮口闪光
* 炮弹尾迹
* 爆炸圆环
* 火花粒子
* 烟雾粒子
* 地形碎片
* 浮动伤害数字
* 坦克受击闪烁
* 镜头震动
* 回合提示动画
* 按钮点击反馈
* 生命值变化动画

粒子数量要根据设备性能动态调整。

移动端低性能模式下：

* 降低粒子数量
* 降低烟雾持续时间
* 降低地形碎片数量
* 降低背景动画复杂度
* 限制设备像素比
* 保持游戏逻辑准确

---

# 十七、音频系统

使用 Web Audio API 创建简单原创音效或程序化音效。

至少包含：

* 按钮点击
* 炮弹发射
* 炮弹飞行
* 爆炸
* 坦克移动
* 坦克受伤
* 回合切换
* 游戏胜利

要求：

* 首次用户交互后初始化 AudioContext。
* 页面进入后台时暂停不必要的声音。
* 支持主音量、音乐音量、音效音量。
* 音频设置保存至 localStorage。
* 没有音频资源时游戏仍能正常运行。
* 不得使用或模仿原游戏音效文件。

---

# 十八、性能要求

目标性能：

* 普通电脑 Chrome：稳定接近 60 FPS。
* 中端安卓设备新版 Chrome：尽量保持 50–60 FPS。
* 较弱安卓设备：不得长期低于 30 FPS。
* 页面不能持续产生大量临时对象导致频繁 GC 卡顿。
* 不得每帧调用大范围 `getImageData`。
* 粒子使用对象池。
* 炮弹使用对象池或安全回收机制。
* 使用 `requestAnimationFrame`。
* 游戏逻辑使用 delta time。
* 必要时使用固定物理步长并限制单帧最大补算次数。
* 页面不可见时暂停主循环或降低更新频率。
* 长时间游玩不能出现明显内存持续增长。
* 重新开始多局后不能重复注册事件监听器。

---

# 十九、代码架构

代码必须模块化，禁止把整个游戏写在单个文件中。

建议目录结构：

```text
project-root/
├─ index.html
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
├─ public/
│  ├─ manifest.webmanifest
│  ├─ icons/
│  └─ sw.js
└─ src/
   ├─ main.ts
   ├─ styles/
   │  ├─ reset.css
   │  ├─ game.css
   │  └─ mobile.css
   ├─ core/
   │  ├─ Game.ts
   │  ├─ GameLoop.ts
   │  ├─ SceneManager.ts
   │  ├─ EventBus.ts
   │  └─ Constants.ts
   ├─ scenes/
   │  ├─ MainMenuScene.ts
   │  ├─ BattleScene.ts
   │  ├─ PauseScene.ts
   │  └─ ResultScene.ts
   ├─ entities/
   │  ├─ Tank.ts
   │  ├─ Projectile.ts
   │  └─ Particle.ts
   ├─ systems/
   │  ├─ TurnManager.ts
   │  ├─ InputManager.ts
   │  ├─ MobileControls.ts
   │  ├─ TerrainSystem.ts
   │  ├─ ProjectileSystem.ts
   │  ├─ CollisionSystem.ts
   │  ├─ DamageSystem.ts
   │  ├─ WeaponSystem.ts
   │  ├─ CameraSystem.ts
   │  ├─ ParticleSystem.ts
   │  ├─ AudioSystem.ts
   │  └─ SaveSystem.ts
   ├─ weapons/
   │  ├─ WeaponDefinition.ts
   │  ├─ WeaponRegistry.ts
   │  └─ behaviors/
   ├─ rendering/
   │  ├─ Renderer.ts
   │  ├─ BackgroundRenderer.ts
   │  ├─ TerrainRenderer.ts
   │  └─ HudRenderer.ts
   ├─ ui/
   │  ├─ MainMenu.ts
   │  ├─ BattleHud.ts
   │  ├─ TouchControls.ts
   │  ├─ SettingsPanel.ts
   │  └─ ResultPanel.ts
   ├─ config/
   │  ├─ gameConfig.ts
   │  └─ weaponConfig.ts
   ├─ types/
   │  └─ index.ts
   └─ utils/
      ├─ math.ts
      ├─ random.ts
      ├─ geometry.ts
      ├─ device.ts
      └─ objectPool.ts
```

可以根据实际实现略微调整，但必须维持清晰的模块职责。

---

# 二十、编程质量要求

必须遵守：

1. 开启 TypeScript 严格模式。
2. 不使用大量 `any`。
3. 为重要数据结构编写 interface 或 type。
4. 重要算法添加简短注释。
5. 不写无意义的注释。
6. 避免超长函数。
7. 避免循环依赖。
8. 所有事件监听都必须能够移除。
9. 所有计时器都必须能够清理。
10. 切换场景时清理旧场景资源。
11. 游戏重新开始时彻底重置状态。
12. 不留下 `TODO`、占位函数或空实现。
13. 不使用伪代码代替关键逻辑。
14. 不隐藏构建错误。
15. 不省略 CSS。
16. 不省略移动端触控代码。
17. 不省略 PWA 文件。
18. 不省略地形碰撞与破坏代码。
19. 不把核心功能描述为“之后可以实现”。
20. 所有引用的文件都必须真正提供。

---

# 二十一、可访问性要求

需要考虑：

* 重要按钮有文本标签。
* 只使用图标的按钮必须有 `aria-label`。
* 玩家区分不能只依赖颜色。
* 支持键盘操作。
* 焦点状态清晰可见。
* 文字与背景有足够对比度。
* 可关闭屏幕震动。
* 可降低动画效果。
* 尊重 `prefers-reduced-motion`。
* 音量可以调节或静音。

---

# 二十二、PWA 与离线要求

实现可安装 PWA。

必须提供：

* `manifest.webmanifest`
* Service Worker
* 应用图标占位方案
* 离线缓存
* 基础启动页面
* 正确的主题颜色
* Android Chrome 安装支持

离线规则：

* 首次完整加载后，即使断网也可以进入游戏。
* 游戏运行时不得依赖外部 CDN。
* 不得从远程服务器请求关键资源。
* 所有关键代码和资源必须包含在项目中。
* 外部字体不是必要资源，优先使用系统字体。

---

# 二十三、测试要求

请为核心逻辑提供 Vitest 单元测试。

至少测试：

* 角度和力度转换为初速度。
* 风力影响。
* 爆炸伤害距离衰减。
* 武器弹药扣除。
* 回合切换。
* 胜负判断。
* 随机地图种子复现。
* 坦克出生点合法性。
* 世界边界处理。
* 炮弹超时销毁。
* 两名玩家同时死亡时判定平局。

同时提供一份人工测试清单。

---

# 二十四、必须通过的验收场景

## 场景 1：电脑本地双人

1. 在电脑 Chrome 打开游戏。
2. 玩家 1 使用键盘调整角度和力度。
3. 玩家 1 发射炮弹。
4. 发射后玩家 1 不能再次操作。
5. 炮弹爆炸并修改地形。
6. 伤害结算完成。
7. 自动切换到玩家 2。
8. 玩家 2 使用与玩家 1 完全相同的按键操作。
9. 多个回合后其中一方生命值归零。
10. 正确显示获胜页面。

## 场景 2：安卓 Chrome 横屏

1. 使用新版 Android Chrome 打开游戏。
2. 页面无横向或纵向意外滚动。
3. 触控按钮大小合适。
4. 玩家 1 使用屏幕控制器移动、瞄准和开火。
5. 回合结束后玩家 2 使用同一套屏幕控制器。
6. 两名玩家按钮位置完全一致。
7. 长按按钮能够连续调整。
8. 手指移开或触控取消时操作立即停止。
9. 浏览器地址栏变化后布局仍正常。
10. 游戏保持可玩。

## 场景 3：地形破坏

1. 炮弹命中地面。
2. 地面出现真实弹坑。
3. 弹坑参与后续碰撞。
4. 坦克下方地形被炸掉后坦克下落。
5. 坦克停止在新的地形表面。
6. 坦克不会悬空、穿模或无限抖动。

## 场景 4：离线运行

1. 首次在线打开游戏。
2. 完整加载一次。
3. 关闭网络。
4. 再次打开游戏。
5. 主菜单和完整对战均可正常使用。

## 场景 5：重复开局

1. 连续完成或重新开始 10 局游戏。
2. 控制器没有重复触发。
3. 音效没有重复播放多次。
4. FPS 没有持续下降。
5. 内存没有明显持续增长。
6. 回合状态不会卡死。

---

# 二十五、输出格式

不要只讲设计思路，也不要先给我一个简单 Demo。

请按照下面顺序输出：

## 第一步：项目概述

简要说明：

* 游戏名称
* 核心玩法
* 技术栈
* 架构设计
* 地形破坏方案
* 移动端适配方案

这部分保持简短。

## 第二步：完整目录结构

列出所有项目文件。

## 第三步：逐文件输出完整代码

每个文件使用以下格式：

```text
文件：src/example.ts
```

随后给出完整代码块。

不得使用：

```text
此处省略
其余类似
伪代码
自行补充
根据需要实现
```

## 第四步：安装和运行说明

给出完整命令。

## 第五步：测试说明

包括：

* 自动测试命令
* 桌面端测试方式
* Android Chrome 测试方式
* PWA 离线测试方式

## 第六步：人工验收清单

用复选框形式提供。

---

# 二十六、开发优先级

按照以下优先级完成，低优先级功能不能破坏高优先级功能。

## P0：必须完整实现

* 项目可以启动和构建
* 本地双人回合
* 两名玩家完全相同的操作方式
* 键盘操作
* 安卓触控操作
* 坦克移动
* 炮管角度
* 发射力度
* 重力和风力
* 炮弹飞行
* 地形碰撞
* 可破坏地形
* 爆炸和伤害
* 坦克下落
* 胜负判断
* 响应式布局
* 回合状态机
* 无后端运行

## P1：应完整实现

* 多种武器
* 摄像机跟随
* 粒子效果
* 音效
* 设置保存
* 随机地图种子
* 暂停界面
* 结果统计
* PWA 离线运行
* 单元测试

## P2：完成后优化

* 更丰富的背景
* 更细致的动画
* 动态性能等级
* 更完整的统计信息
* 辅助瞄准效果

如果输出长度受限，必须优先保证 P0 功能形成一个真正可以运行的完整项目。不要为了增加武器数量而省略触控、地形破坏、回合切换或完整工程文件。

---

# 二十七、错误处理要求

必须主动处理：

* Canvas 初始化失败
* AudioContext 初始化失败
* localStorage 不可用
* Service Worker 注册失败
* 页面切后台
* 屏幕旋转
* 指针操作被取消
* 炮弹飞出世界
* 炮弹存在时间过长
* 坦克没有找到合法落地点
* 地图生成失败
* 玩家出生位置不合法
* 当前武器弹药耗尽
* 回合状态异常
* 重复点击发射按钮
* 游戏结束后仍接收控制输入

错误应尽量恢复，不要因为非关键功能失败导致整个游戏无法运行。

---

# 二十八、最终自检

在输出代码前，请自行逐项检查：

* 是否提供了 package.json？
* 是否提供了所有 import 对应的文件？
* 是否存在错误的文件路径？
* 是否存在未定义变量？
* 是否存在 TypeScript 类型错误？
* 是否存在引用但未实现的方法？
* 是否正确清理事件监听？
* 是否真正实现地形破坏？
* 是否真正实现安卓触控？
* 是否确保两名玩家使用同一套操作？
* 是否真正实现回合锁定？
* 是否能在炮弹结束后正常切换玩家？
* 是否提供了 PWA 文件？
* 是否没有使用外部受版权保护的素材？
* 是否没有复制现有游戏名称和视觉设计？
* 是否可以通过 `npm install` 和 `npm run dev` 启动？

发现问题时先修复，再输出最终代码。

现在开始创建完整项目。不要只回复计划，直接给出项目概述、目录和全部代码。
